# Clerk Authentication
# Get your keys from: https://dashboard.clerk.com/
# 1. Create a new application (or use existing)
# 2. Go to API Keys section
# 3. Copy Publishable Key and Secret Key
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_****
CLERK_SECRET_KEY=sk_test_****

# Clerk URL Configuration (optional - defaults shown below)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/register
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# AI Gateway API Key (required for non-Vercel deployments)
# For Vercel deployments, OIDC tokens are used automatically
# Note: This key is also used by the Mastra CSV analysis feature
AI_GATEWAY_API_KEY=****

# Optional: override the Gateway base URL when using a custom domain
# AI_GATEWAY_URL=https://your-custom-gateway.vercel.sh/v1/ai


# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
POSTGRES_URL=****


# Redis (optional in development, required for resumable chat streams)
# Production: Provision Vercel Redis and copy the TLS-enabled connection string
# Local development: `docker run -p 6379:6379 redis:7-alpine` then use `redis://localhost:6379`
# Docs: https://vercel.com/docs/storage/vercel-redis/quickstart
REDIS_URL=****

# Xero Integration
# Get your Xero API credentials from: https://developer.xero.com/
# 1. Create a new app (Custom Connection type)
# 2. Copy Client ID and Client Secret
# 3. Set Redirect URI to: {YOUR_DOMAIN}/api/xero/callback
XERO_CLIENT_ID=****
XERO_CLIENT_SECRET=****
XERO_REDIRECT_URI=http://localhost:3000/api/xero/callback

# Encryption key for Xero tokens (generate with: openssl rand -hex 32)
XERO_ENCRYPTION_KEY=****

# Cron Job Secret
# Generate with: openssl rand -base64 32
# Add to Vercel: Project Settings → Environment Variables → CRON_SECRET
CRON_SECRET=****

# Firecrawl API Key
# Get your API key from: https://firecrawl.dev/
# Used for web scraping regulatory sources in the Q&A agent
FIRECRAWL_API_KEY=****
